# Portainer Stack — Git-Deploy on Synology DS725+
# Environment variables are set in Portainer UI, not in this file.
services:
  mybaby:
    build: .
    container_name: mybaby
    restart: unless-stopped
    ports:
      - "8080:8000"
    volumes:
      - /volume2/docker/mybaby/data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY}
      - AUTH_MODE=${AUTH_MODE:-forward}
      - AUTH_TRUSTED_HEADER=${AUTH_TRUSTED_HEADER:-Remote-User}
      - AUTH_TRUSTED_PROXIES=${AUTH_TRUSTED_PROXIES:-192.168.178.0/24}
      - DATABASE_URL=${DATABASE_URL:-sqlite:////app/data/mybaby.db}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - ENVIRONMENT=${ENVIRONMENT:-prod}
    stop_grace_period: 15s
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - nginx-proxy_default

networks:
  nginx-proxy_default:
    external: true
